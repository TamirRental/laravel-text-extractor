<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('document_extractions', function (Blueprint $table): void {
            $table->id();
            $table->string('type');
            $table->string('filename');
            $table->string('identifier');
            $table->json('extracted_data');
            $table->json('metadata')->nullable();
            $table->string('status');
            $table->text('error_message')->nullable();
            $table->string('external_task_id')->nullable();
            $table->string('provider');
            $table->string('file_path')->nullable();
            $table->timestamps();

            $table->index(['type', 'filename']);
            $table->index(['type', 'identifier']);
            $table->unique(['external_task_id', 'provider']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('document_extractions');
    }
};
